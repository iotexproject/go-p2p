name: Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
<<<<<<< HEAD
        go-version: 1.18.5
=======
        go-version: 1.21.4
>>>>>>> upstream/master

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v -race ./...
